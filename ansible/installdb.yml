---
- hosts: db
  become: yes

  tasks:
    - name: 'Instalação de pacotes do banco de dados'
      apt:
        pkg:
          - mariadb-server
          - wget
          - zip
          - curl
          - vim
          - python3-mysqldb
        state: present
        update_cache: yes

    - name: 'Criação do banco de dados backup'
      mysql_db:
        name: backup
        state: present
        login_unix_socket: /run/mysqld/mysqld.sock

    - name: 'Verificar se o banco já foi extraído'
      stat:
        path: /tmp/backup.sql
      register: db_existente

    - name: 'Download e extração do backup do banco de dados'
      unarchive:
        src: http://forceclass-00.linuxforce.com.br/express/backup.zip
        dest: /tmp
        remote_src: yes
      when: not db_existente.stat.exists

    - name: 'Importação da base de dados (backup.sql)'
      mysql_db:
        state: import
        name: backup
        login_unix_socket: /run/mysqld/mysqld.sock
        target: /tmp/backup.sql

    - name: 'Criação do usuário Express no MySQL'
      mysql_user:
        host: '%'
        name: express
        password: 'AllSafe0!'
        priv: 'backup.*:ALL'
        state: present
        login_unix_socket: /run/mysqld/mysqld.sock

    - name: 'Ajuste do bind-address no MySQL'
      replace:
        path: /etc/mysql/mariadb.conf.d/50-server.cnf
        regexp: '127\.0\.0\.1'
        replace: '0.0.0.0'
      notify: RestartMysql  

  handlers:
    - name: RestartMysql
      service:
        name: mysql
        state: restarted
        enabled: yes
